<!-- SPDX-License-Identifier: Apache-2.0 -->

# v0.2.3 Publication Verification

The engine/homepage release merged through PR #1430 at
`79f1ad6d96345d9ef9b36b89159e4d9ee673fb9c`, after all 40 checks passed.
The production homepage passed the nine-viewport/eighteen-anchor browser matrix,
all controls, and reduced motion; sampled animation painted on 85/85 frames with
zero idle geometry reads. Its JavaScript SHA-256 matches the checked-in asset:
`03726c362c78e3f53a45de1fb6d717b2ce96740619e3c17d441c5f14a5bcd0fd`.
Cloudflare's injected analytics beacon is blocked by the existing CSP; the
homepage's own scripts and interactions pass without relaxing that policy.

## GitHub

[v0.2.3](https://github.com/depsilon/shardloom/releases/tag/v0.2.3) is published.
The [fresh channel transcript](channel-proofs/github-prerelease-v0.2.3-transcript.json)
checks all twelve downloaded assets against GitHub's digests and the checksum
manifest. A clean downloaded-wheel install resolves its bundled native CLI and
returns exact typed values through DataFrame collection and two consecutive SQL
executions. Unsupported execution stays explicit. CLI/package uninstall checks
passed. The tag targets a GitHub-verified merge commit; local assets are unsigned.

## Registry Build Recipe

The first TestPyPI attempt, workflow `33978578712`, was cancelled before publication
after identifying an existing macOS wheel-label defect. Wheel's macOS platform
recalculation can discard the package hook's post-finalization assignment and use
the Python interpreter's universal2 label, despite a single-architecture CLI.

The release staging recipe now supplies an explicit bdist_wheel platform option
and aligns the existing package override with that option. It conservatively
uses the build host's macOS major release and actual architecture. No engine,
Python runtime, package setup source, published GitHub asset, or release tag is
changed by this recipe correction.

Real build reproduction with `_PYTHON_HOST_PLATFORM=macosx-10.13-universal2`:

- Before: `shardloom-0.2.3-cp313-cp313-macosx_10_13_universal2.whl`.
- After: `shardloom-0.2.3-cp313-cp313-macosx_26_0_arm64.whl`, with matching WHEEL
  metadata, successful clean install, bundled-CLI smoke, and uninstall.
- Python discovery: 786 tests, 144 existing skips, no failures. Focused tests cover
  arm64, x86_64, macOS major-version normalization, preserved configuration,
  non-macOS no-op behavior, invalid platform failure, and package builder options.

TestPyPI, PyPI, and Homebrew require their fresh transcripts before the selected
published-channel contract advances. The fixed 4 GiB target has been dropped by
the maintainer. Other performance phases remain open; no new timing claim is
created by package publication.
